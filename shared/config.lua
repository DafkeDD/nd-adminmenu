Config = Config or {}
-- ox_lib v3.6.1 or better
-- As this time the ban system are not even exsist So use something else!
--Locale must be set via ox_Lib in server cfg check for "setr ox:locale en" if not found put it in!
Config.menukeybind = 'INSERT' --Default Open Key! (This uses ox_lib keymapping functonality so if its not working do not blame!)
Config.menucommand = 'menu' --Command to open the admin menu!